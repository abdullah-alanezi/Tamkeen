<nav class="navbar navbar-expand-lg top-navbar px-4">
    <div class="container-fluid d-flex align-items-center">
        
        <div class="d-none d-md-block">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-0" style="--bs-breadcrumb-divider: '›';">
                    <li class="breadcrumb-item"><a href="#" class="text-decoration-none text-muted small">Platform</a></li>
                    <li class="breadcrumb-item active small text-primary fw-semibold" aria-current="page">@ViewData["Title"]</li>
                </ol>
            </nav>
        </div>

        <div class="ms-auto d-flex align-items-center gap-3">
            
            <div class="d-none d-lg-flex position-relative align-items-center me-2">
                <i class="bi bi-search position-absolute ms-3 text-muted"></i>
                <input type="text" class="form-control form-control-sm ps-5 bg-light border-0 rounded-pill" placeholder="Search data..." style="width: 250px;">
            </div>

            <button class="btn btn-link text-dark position-relative p-2 rounded-circle hover-bg-light">
                <i class="bi bi-bell fs-5"></i>
                <span class="position-absolute top-0 start-100 translate-middle p-1 bg-danger border border-light rounded-circle">
                    <span class="visually-hidden">New alerts</span>
                </span>
            </button>

            <div class="vr mx-2 opacity-10 d-none d-sm-block" style="height: 24px;"></div>

            <div class="dropdown">
                <a href="#" class="d-flex align-items-center text-decoration-none dropdown-toggle text-dark" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    <div class="me-2 text-end d-none d-sm-block">
                        <div class="fw-bold small lh-1">Ahmed Ali</div>
                        <small class="text-muted" style="font-size: 0.7rem;">System Admin</small>
                    </div>
                    <img src="https://ui-avatars.com/api/?name=Ahmed+Ali&background=4e73df&color=fff" 
                         width="38" height="38" 
                         class="rounded-circle shadow-sm border border-2 border-white" alt="Profile">
                </a>
                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-3 rounded-4" aria-labelledby="userDropdown">
                    <li class="px-3 py-2 border-bottom mb-2 d-sm-none">
                        <div class="fw-bold">Ahmed Ali</div>
                        <div class="text-muted small">System Admin</div>
                    </li>
                    <li><a class="dropdown-item py-2 px-3 rounded-3 mx-2 w-auto" href="#"><i class="bi bi-person me-2 text-primary"></i> My Profile</a></li>
                    <li><a class="dropdown-item py-2 px-3 rounded-3 mx-2 w-auto" href="#"><i class="bi bi-gear me-2 text-primary"></i> Settings</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item py-2 px-3 rounded-3 mx-2 w-auto text-danger" href="#"><i class="bi bi-box-arrow-right me-2"></i> Sign Out</a></li>
                </ul>
            </div>
        </div>
    </div>
</nav>

<style>
    /* Styling to complement the Layout CSS */
    .top-navbar {
        background: rgba(255, 255, 255, 0.9) !important;
        backdrop-filter: blur(8px);
        transition: all 0.3s ease;
    }

    .hover-bg-light:hover {
        background-color: #f8f9fa;
        color: var(--tk-primary) !important;
    }

    .dropdown-menu {
        min-width: 200px;
    }

    .dropdown-item:active {
        background-color: var(--tk-primary);
    }
    
    .breadcrumb-item + .breadcrumb-item::before {
        color: #cbd5e1;
    }
</style>